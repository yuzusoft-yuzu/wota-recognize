# ===== Wota艺 动作识别系统 (骨光融合版) 核心依赖 =====
flask
numpy
# 注意：mediapipe 强制依赖 opencv-contrib-python（GUI 版）而非 headless，
# 故此处统一用 contrib 版，避免两包并存时 cv2 目录互相覆盖。
# 服务器需安装系统库：libgl1 libglib2.0-0 libsm6 libxext6 libxrender1 libgomp1
opencv-contrib-python
scipy

# ===== 骨骼融合 (A路 MediaPipe 骨骼) =====
# 已启用：安装后系统以「骨光融合」模式运行；未安装时自动降级为「纯光棒模式」。
mediapipe>=1.0.1

# ===== 生产部署 (Linux) =====
gunicorn
