# ===== Wota艺 动作识别系统 (骨光融合版) 核心依赖 =====
flask
numpy
opencv-python-headless
scipy

# ===== 可选：启用「骨骼融合」(A路 MediaPipe 骨骼) =====
# 未安装时系统自动降级为「纯光棒模式」(仅 B路 OpenCV 光棒)，功能仍可用。
# 注意：mediapipe 对 Python 版本与 numpy 2.x 兼容性有要求，按需安装。
# mediapipe

# ===== 可选：生产部署 (Linux) =====
# gunicorn
